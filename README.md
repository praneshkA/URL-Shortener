# URL-Shortener
SnapLink Analytics is a full-stack URL shortener application that allows users to generate short URLs, manage links, and track analytics such as click count and recent visits. The platform includes secure authentication, a responsive dashboard, server-side redirection, and analytics visualization using React, Node.js, Express, and MongoDB.
